{
  "name": "claude-swarm",
  "version": "0.2.7",
  "private": true,
  "description": "Smoke tests for the claude-swarm Claude Code plugin. The plugin itself needs no build step; this exists only to validate the manifests, hook, and workflows load.",
  "scripts": {
    "test": "node test/smoke.js"
  },
  "license": "MIT"
}
