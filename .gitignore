# Benchmark output: timestamped run results, transcripts, and reports.
# Regenerated by `node evals/run.js`; the cases and runner are what's tracked.
evals/results/

node_modules/

# Appended by the SubagentStop completion-feed hook in any project the plugin
# is enabled in — including this one, whenever its own subagents run.
.claude-swarm-feed.log
